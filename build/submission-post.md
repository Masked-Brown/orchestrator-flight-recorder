# Submission post — draft

Two to three sentences plus the link, per the brief. Three variants; the first is the one to
post unless something about the thread suggests otherwise.

---

## A — the one to use

> **orchestrator-flight-recorder** — you tell it what went wrong in an AI build session, you
> give it your Claude data export, and it reads the black box: the exact message where the fault
> entered, how it travelled to the point you noticed, and one probable cause, quoting your own
> conversation back at you. Then it stops — no rewritten prompts, no fixes, and a script blocks
> them. For anyone who runs long orchestrated sessions and has ended one wondering what actually
> went wrong; three real investigations ship with it, graded against what the participants
> themselves said next.
>
> <REPO_URL>

## B — shorter, if the thread is moving fast

> **orchestrator-flight-recorder** diagnoses why an AI-orchestrated build session failed, from
> that session's own Claude export: one probable cause, anchored to your own words, and it stops
> at cause rather than handing you fixes. Three real blind runs ship with it — and two of the
> three verdicts came back against what the build plan predicted, in opposite directions.
>
> <REPO_URL>

## C — leads on the evidence, if the field is heavy on demos

> **orchestrator-flight-recorder** — an air accident investigator for AI working sessions. Feed
> it your Claude export and one line about what went wrong; it names the message where the fault
> entered, traces it to where you noticed, and ends at cause. Every quote is verified verbatim by
> a blocking gate, and the three shipped runs are real incidents from my own week, one of which
> the investigation demonstrably got partly wrong — which is in the README rather than hidden.
>
> <REPO_URL>

---

**Before posting:** replace `<REPO_URL>` and confirm the repository is public. `OPEN-DEFECTS.md`
is the honest list if anyone asks what it does not do.
